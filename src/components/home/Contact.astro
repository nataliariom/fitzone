---
const days = ['Martes', 'Miércoles', 'Jueves', 'Viernes', 'Sábado'];
---

<section class="d2c_gradient_tl" id="d2c_contact">
  <div class="container">
    <div class="text-center w-full md:w-11/12 lg:w-4/5 xl:w-[60%] mx-auto">
       <h2 class="d2c_title">
        Solicita tu clase <br />
        <span class="text-outline" data-text="Gratis"> Gratis</span>
      </h2>
      <p class="w-full md:w-4/5 xl:w-3/4 mx-auto">
        Solo llena el formulario y nos pondremos en contacto contigo. ¡Es ahora o nunca!
      </p>
    </div>

    <!-- Contenido -->
    <div class="grid grid-cols-12 gap-4 md:gap-6 mt-10 xl:mt-14">
      <!-- Formulario -->
      <div class="col-start-1 col-span-12 lg:col-span-8">
        <form
          class="space-y-8"
          id="contactForm"
          data-aos="fade-up"
          action="https://formspree.io/f/xjkoanjn"
          method="POST"
        >
          <div class="grid grid-cols-1 gap-8 sm:grid-cols-6">
            <div class="sm:col-span-3">
              <label class="form-label">Nombre completo</label>
              <input type="text" name="name" class="form-control d2c_card_bg" placeholder="Ingresa tu nombre completo" required />
            </div>
            <div class="sm:col-span-3">
              <label class="form-label">Correo electrónico</label>
              <input type="email" name="email" class="form-control d2c_card_bg" placeholder="ejemplo@correo.com" required />
            </div>
          </div>

          <button type="submit" class="d2c_btn d2c_btn_secondary">Solicitar clase gratis</button>
          <p id="formSuccess" class="text-green-400 font-medium mt-2 hidden">¡Gracias! Te contactaremos pronto con tu código.</p>
        </form>

      </div>

       <!-- Horarios -->
      <div class="col-start-1 col-span-12 lg:col-start-9 lg:col-span-4">
        <div class="mt-5 md:mt-4 lg:mt-0 py-8 px-6 border border-greenish-gray rounded-lg space-y-10">
          <h4 class="text-3xl uppercase font-normal font-primary mb-2 bg-gradient-to-b from-primary to-secondary text-transparent bg-clip-text border-b border-b-greenish-gray pb-2">
            Horarios de entrenamiento
          </h4>
          <ul class="d2c_training_hours">
            {days.map(day => (
              <li class="border-b border-b-greenish-gray pt-4 pb-2 flex gap-3 justify-between">
                <span class="w-3/5">{day}</span>
                <span class="w-2/5 text-end">07:00 - 21:00</span>
              </li>
            ))}
          </ul>
        </div>
      </div>
    </div>
  </div>


  <!-- Toast: Éxito -->
<div id="toastSuccess" class="fixed top-6 right-6 z-50 bg-green-600 text-white px-5 py-3 rounded-md shadow-md hidden opacity-0 transition-opacity duration-300">
  ✅ ¡Mensaje enviado correctamente!
</div>

<!-- Toast: Error -->
<div id="toastError" class="fixed top-6 right-6 z-50 bg-red-600 text-white px-5 py-3 rounded-md shadow-md hidden opacity-0 transition-opacity duration-300">
  ❌ Ocurrió un error. Intenta de nuevo.
</div>

</section>
<script is:inline>
  const form = document.getElementById("contactForm");
  const toastSuccess = document.getElementById("toastSuccess");
  const toastError = document.getElementById("toastError");

  form?.addEventListener("submit", async (e) => {
    e.preventDefault();

    const formData = new FormData(form);

    try {
      const response = await fetch(form.action, {
        method: "POST",
        body: formData,
        headers: {
          Accept: "application/json",
        },
      });

      if (response.ok) {
        form.reset();
        showToast(toastSuccess);
      } else {
        showToast(toastError);
      }
    } catch (error) {
      showToast(toastError);
    }
  });

  function showToast(toast) {
    toast.classList.remove("hidden");
    setTimeout(() => toast.classList.add("opacity-100"), 10);

    setTimeout(() => {
      toast.classList.remove("opacity-100");
      setTimeout(() => toast.classList.add("hidden"), 300);
    }, 3000);
  }
</script>
